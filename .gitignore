# data: track the CSV mirror (small, text, perishable snapshot), but never the
# binary DuckDB file (regenerable from the CSVs / `make data`).
data/*.duckdb
*.duckdb
*.duckdb.wal

# python
.venv/
__pycache__/
*.pyc

# soccerdata cache
.soccerdata/

# env
.env
